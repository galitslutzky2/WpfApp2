<Window x:Class="WpfApp2.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- פס טעינה -->
            <Border Width="300" Height="25" CornerRadius="12.5" BorderThickness="1" BorderBrush="#DDD" Padding="2">
                <Grid x:Name="ProgressGrid" ClipToBounds="True">
                    <Rectangle RadiusX="10" RadiusY="10">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#222" Offset="0"/>
                                <GradientStop Color="#555" Offset="0.2"/>
                                <GradientStop Color="#888" Offset="0.4"/>
                                <GradientStop Color="#AAA" Offset="0.6"/>
                                <GradientStop Color="#DDD" Offset="0.8"/>
                                <GradientStop Color="#FFF" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                        <Rectangle.RenderTransform>
                            <TranslateTransform x:Name="ProgressTransform" X="-300"/>
                        </Rectangle.RenderTransform>
                    </Rectangle>

                    <!--אנימציה להזזת הגרדיאנט-->
                    <Grid.Triggers>
                        <EventTrigger RoutedEvent="Grid.Loaded">
                            <BeginStoryboard>
                                <Storyboard >
                                    <DoubleAnimation Storyboard.TargetName="ProgressTransform"
                                      Storyboard.TargetProperty="X"
                                      From="-300" To="0" Duration="0:0:5" 
                                      AutoReverse="False"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Grid.Triggers>
                </Grid>
            </Border>
            <!-- טקסט מתחת -->
            <TextBlock Text="LOADING..." 
HorizontalAlignment="Center" 
FontSize="20" FontWeight="SemiBold" 
Margin="0,20,0,0" 
Foreground="Black" />
        </StackPanel>
    </Grid>
</Window>
